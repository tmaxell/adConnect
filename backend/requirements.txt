# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.7.0

# ── LLM core (provider packages installed on demand, see llm.py) ──────────────
langchain-core>=0.2.0

# Optional LLM providers — install the one matching LLM_PROVIDER in .env:
#   langchain-google-genai>=1.0.0    # gemini  (recommended free tier)
#   langchain-groq>=0.1.0            # groq
#   langchain-gigachat>=0.3.0        # gigachat (Sber)
#   langchain-ollama>=0.1.0          # ollama  (local)
#   langchain-anthropic>=0.3.2       # anthropic

# ── SQL persistence (chat sessions / runs / artifacts) ────────────────────────
sqlalchemy>=2.0
aiosqlite>=0.19
greenlet>=3.0                        # required by sqlalchemy.ext.asyncio
asyncpg>=0.29                        # optional Postgres driver

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
